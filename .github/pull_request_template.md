## What & why

<!-- What does this change and why? Link any issue. -->

## How I tested

- [ ] `python3 -m pytest -q tests` passes (paste the count)
- [ ] Swift builds (`cd macapp && swift build`) — if the app/client changed
- [ ] Tested against real hardware? If so, which model + CE version:

## Checklist

- [ ] Protocol changes are reflected in **both** Python and Swift
- [ ] Added/updated tests
- [ ] Updated `CHANGELOG.md` (`[Unreleased]`) for user-visible changes
- [ ] No new runtime dependencies
- [ ] Significant AI assistance disclosed (if any)
